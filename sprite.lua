--sprite.lua

require 'class'

--I want drawing to be it's own thing, but I can't decide how. we could take all the non
--grid stuff out of fighter, that would be good. but movement and shit will change.
--whatever
--Sprite = class(
--    function(s, px, py, width, height, speed, color)
--        s.px = px
--        s.py = py
--        s.dx = px
--        s.dy = py
--        s.width = width
--        s.height = height
--        s.speed = speed
--        s.color = color or {255,255,255}
--    end
--)